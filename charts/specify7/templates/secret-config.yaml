apiVersion: v1
kind: Secret
metadata:
  name: {{ include "specify7.fullname" . }}-config
  labels:
    {{- include "specify7.labels" . | nindent 4 }}
type: Opaque
stringData:
  local_specify_settings.py: |
    # Generated by Helm — env vars (from secrets.existingSecret) override these defaults.
    import os

    DATABASE_NAME = os.environ.get('DB_NAME', '{{ .Values.mariadb.auth.database }}')
    {{- if .Values.mariadb.enabled }}
    DATABASE_HOST = '{{ .Release.Name }}-mariadb'
    {{- else }}
    DATABASE_HOST = os.environ.get('DB_HOST', '{{ .Values.specify.database.host }}')
    {{- end }}
    DATABASE_PORT = int(os.environ.get('DB_PORT', '{{ .Values.specify.database.port }}'))

    MASTER_NAME = os.environ.get('DB_USER', '{{ .Values.mariadb.auth.username }}')
    MASTER_PASSWORD = os.environ.get('DB_PASSWORD', '{{ .Values.mariadb.auth.password }}')

    APP_USER_NAME = MASTER_NAME
    APP_USER_PASSWORD = MASTER_PASSWORD

    SECRET_KEY = os.environ.get('SECRET_KEY', '{{ .Values.specify.secretKey }}')
    WEB_ATTACHMENT_KEY = os.environ.get('ATTACHMENT_KEY', '{{ .Values.specify.attachmentKey }}')

    _debug_env = os.environ.get('DEBUG', '').lower()
    DEBUG = (
        _debug_env in ('1', 'true', 'yes') if _debug_env
        else {{ .Values.specify.debug | ternary "True" "False" }}
    )
    ALLOWED_HOSTS = {{ toJson .Values.specify.allowedHosts }}
    CSRF_TRUSTED_ORIGINS = {{ toJson .Values.specify.csrfTrustedOrigins }}

    STATIC_ROOT = '/volumes/static-files'

  secret_key.py: |
    import os
    SECRET_KEY = os.environ.get('SECRET_KEY', '{{ .Values.specify.secretKey }}')

  debug.py: |
    import os
    _debug_env = os.environ.get('DEBUG', '').lower()
    DEBUG = (
        _debug_env in ('1', 'true', 'yes') if _debug_env
        else {{ .Values.specify.debug | ternary "True" "False" }}
    )
